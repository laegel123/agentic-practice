# 테스트 전용 프로파일. 실제 파일 DB(~/legacyshopdb)를 건드리지 않도록 인메모리 H2 를 쓴다.
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
